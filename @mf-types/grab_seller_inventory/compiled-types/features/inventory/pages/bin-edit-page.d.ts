export type EditBinPageProps = {
    params: Promise<{
        id: string;
        zoneId: string;
        binId: string;
    }>;
};
export default function EditBinPage(): import("react").JSX.Element;
