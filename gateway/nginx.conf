server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location = /mfe/product/mf-manifest.json {
    proxy_pass http://seller-product/mf-manifest.json;
    add_header Cache-Control "no-store" always;
  }

  location = /mfe/inventory/mf-manifest.json {
    proxy_pass http://seller-inventory/mf-manifest.json;
    add_header Cache-Control "no-store" always;
  }

  location /mfe/product/assets/ {
    proxy_pass http://seller-product/assets/;
    add_header Cache-Control "public, max-age=31536000, immutable" always;
  }

  location /mfe/inventory/assets/ {
    proxy_pass http://seller-inventory/assets/;
    add_header Cache-Control "public, max-age=31536000, immutable" always;
  }

  location /mfe/product/ {
    proxy_pass http://seller-product/;
    add_header Cache-Control "no-cache" always;
  }

  location /mfe/inventory/ {
    proxy_pass http://seller-inventory/;
    add_header Cache-Control "no-cache" always;
  }

  location /api/ {
    proxy_pass http://grab-store-dev:8080/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location = /runtime-config.json {
    add_header Cache-Control "no-store" always;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }

  location = /index.html {
    add_header Cache-Control "no-cache" always;
  }
}
